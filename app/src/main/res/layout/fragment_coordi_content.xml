<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".presentation.posting.CoordiContentFragment">

        <FrameLayout
            android:layout_marginTop="50dp"
            android:id="@+id/layout_posting_add"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:background="@color/grey_fa"
            app:layout_constraintDimensionRatio="358:206"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintWidth_percent="0.91">

            <TextView
                android:id="@+id/tv_posting_add_image"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:drawablePadding="9dp"
                android:gravity="center"
                android:text="내용을 입력해주세요"
                android:textColor="@color/grey_ae"
                android:textSize="14sp"
                app:drawableTopCompat="@drawable/ic_baseline_create_24" />

        </FrameLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>